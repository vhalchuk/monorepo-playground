{
  "name": "@my-repo/shared",
  "version": "1.0.0",
  "main": "dist/index",
  "types": "dist/index",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "npm run clean && npm run compile",
    "clean": "rimraf ./dist",
    "compile": "tsc -p tsconfig.build.json"
  },
  "devDependencies": {
    "rimraf": "~6.0.0",
    "typescript": "~4.9.0"
  }
}